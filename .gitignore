# Python
*.pyc
__pycache__/
*.egg-info/

# Node / build artifacts
node_modules/
dist/
hrms/public/dist
hrms/public/node_modules

# Editors / OS
.DS_Store
*.swp
.vscode
.vs
tags

# Local DB dumps & site backups (contain credentials and employee data — keep local only)
docker/backups/
hrms/docker/backups/
*.sql.gz
*-site_config_backup.json

# Production credentials – NEVER commit the real .env
deploy/.env
deploy/server.env
deploy/envs/*.env
!deploy/envs/*.env.example
deploy/compose/.image.env
backups/

# Claude Code agent working directory (worktrees, session data).
# Everything under .claude/ is ignored EXCEPT the shared, reviewed team assets
# (agent and skill definitions), which are committed so the whole team gets them.
.claude/*
!.claude/agents/
!.claude/skills/

# Logs
*debug.log
branch_structure.json
temp_auto_push.bat
temp_interactive_push.bat
